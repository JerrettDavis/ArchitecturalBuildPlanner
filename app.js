let scene,camera,renderer,controls;class WallAssembly{constructor(length,height,spacing){this.length=length;this.height=height;this.spacing=spacing/12;}build(){const studs=[];for(let x=0;x<=this.length;x+=this.spacing){studs.push({x,height:this.height});}return{studs};}}function init3D(){const canvas=document.getElementById('three');renderer=new THREE.WebGLRenderer({canvas,antialias:true});renderer.setSize(canvas.clientWidth,canvas.clientHeight);scene=new THREE.Scene();scene.background=new THREE.Color(0xf0f0f0);camera=new THREE.PerspectiveCamera(60,canvas.clientWidth/canvas.clientHeight,0.1,1000);camera.position.set(10,6,10);controls=new THREE.OrbitControls(camera,renderer.domElement);const light=new THREE.DirectionalLight(0xffffff,1);light.position.set(10,20,10);scene.add(light);scene.add(new THREE.AmbientLight(0xffffff,0.4));}function render3D(model){while(scene.children.length>2){scene.remove(scene.children[2]);}const mat=new THREE.MeshStandardMaterial({color:0x8b5a2b});model.studs.forEach(s=>{const mesh=new THREE.Mesh(new THREE.BoxGeometry(0.125,s.height,0.3),mat);mesh.position.set(s.x-model.studs[model.studs.length-1].x/2,s.height/2,0);scene.add(mesh);});}function render2D(model){const svg=document.getElementById('plan');svg.innerHTML='';model.studs.forEach(s=>{const line=document.createElementNS('http://www.w3.org/2000/svg','line');line.setAttribute('x1',s.x*50);line.setAttribute('x2',s.x*50);line.setAttribute('y1',0);line.setAttribute('y2',300);line.setAttribute('stroke','black');svg.appendChild(line);});}function animate(){requestAnimationFrame(animate);if(controls) controls.update();if(renderer&&scene&&camera){renderer.render(scene,camera);}}function run(){const length=+document.getElementById('length').value;const height=+document.getElementById('height').value;const spacing=+document.getElementById('spacingPreset').value;const wall=new WallAssembly(length,height,spacing);const model=wall.build();render2D(model);render3D(model);document.getElementById('output').textContent=JSON.stringify(model,null,2);}init3D();animate();run();